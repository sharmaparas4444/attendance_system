You are logged in as <b>
    <%= current_employee.email %></b>
<%= link_to('Logout', destroy_employee_session_path, method: :delete) %>
<br><br>
<% if current_employee.checked_in? %>
<%= form_with(url: employees_check_out_path, method: "post") do |form| %>
<%= form.submit 'Check Out'%>
<% end %>
<% else %>
<%= form_with(url: employees_check_in_path, method: "post") do |form| %>
<%= form.submit 'Check In'%>
<% end %>
<% end %>